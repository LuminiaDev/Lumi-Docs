import { Tabs, Callout, Steps } from 'nextra/components'

# Luminesk-CLI

## Installation

Option 1: One-line installer (recommended):

<Tabs items={['Linux and macOS', 'Windows (PowerShell)']}>
  <Tabs.Tab>
    ```bash
    curl -fsSL https://luminesk.taskov1ch.xyz/sh | sh
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    iwr -useb https://luminesk.taskov1ch.xyz/ps1 | iex
    ```
    <Callout type="warning">Run this command in PowerShell</Callout>
  </Tabs.Tab>
</Tabs>

Option 2: Install from PyPI:

```bash
pip install luminesk-cli
```
```bash
uv pip install luminesk-cli
```
```bash
pipx install luminesk-cli
```

<Callout type="info">
    Check Luminesk version after installation:
    ```bash
    nesk --version
    ```
</Callout>

## Create a Lumi server

<Steps>

### Step 1

Update the recipe catalog:
```bash
nesk catalog update
```

### Step 2

Install the Lumi recipe into a new directory:
```bash
nesk install lumi --dir ./server-lumi
```

Luminesk-CLI downloads the recipe, shows the installation plan, and asks for
confirmation. Docker Engine or Docker Desktop must be installed and running.

### Step 3

Start the server:
```bash
nesk start --dir ./server-lumi
```
<Callout type="error">
    Possible errors:
    - `Docker was not found in PATH` - Docker is not installed on this machine. Go to the Docker [download page](https://docs.docker.com/get-started/get-docker/).
    - `Docker failed to pull runtime image` - The initial Docker setup has not been completed. Finish the installation and try again.
</Callout>
</Steps>

## Additional Information
The Luminesk project is an open-source project and is not developed by the Luminia Dev. team. 

- Project website: https://luminesk.taskov1ch.xyz/
- GitHub page: https://github.com/task-v1/luminesk-cli
