import { Tabs, Callout, Steps } from 'nextra/components'

# Luminesk-CLI

## Установка

Вариант 1: Однострочный установщик (рекомендуется):

<Tabs items={['Linux и macOS', 'Windows (PowerShell)']}>
  <Tabs.Tab>
    ```bash
    curl -fsSL https://luminesk.taskov1ch.xyz/sh | sh
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    iwr -useb https://luminesk.taskov1ch.xyz/ps1 | iex
    ```
    <Callout type="warning">Выполните эту команду в PowerShell</Callout>
  </Tabs.Tab>
</Tabs>

Вариант 2: Установка из PyPI:

```bash
pip install luminesk-cli
```
```bash
uv pip install luminesk-cli
```
```bash
pipx install luminesk-cli
```

<Callout type="info">
    После установки проверьте версию Luminesk:
    ```bash
    nesk --version
    ```
</Callout>

## Создание сервера Lumi

<Steps>

### Step 1

Обновите каталог рецептов:
```bash
nesk catalog update
```

### Step 2

Установите рецепт Lumi в новый каталог:
```bash
nesk install lumi --dir ./server-lumi
```

Luminesk-CLI загрузит рецепт, покажет план установки и запросит подтверждение.
Для работы нужен установленный и запущенный Docker Engine или Docker Desktop.

### Step 3

Запустите сервер:
```bash
nesk start --dir ./server-lumi
```
<Callout type="error">
    Возможные ошибки:
    - `Docker was not found in PATH` - На этом компьютере Docker не установлен. Перейдите на [страницу загрузки](https://docs.docker.com/get-started/get-docker/) Docker.
    - `Docker failed to pull runtime image` - Первоначальная настройка Docker не завершена. Завершите установку и попробуйте ещё раз.
</Callout>
</Steps>

## Дополнительная информация
Проект Luminesk является проектом с открытым исходным кодом и не разрабатывается командой Luminia Dev. 

- Веб-сайт проекта: https://luminesk.taskov1ch.xyz/
- Страница на GitHub: https://github.com/task-v1/luminesk-cli
