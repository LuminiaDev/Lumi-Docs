# Введение

Если Minecraft Bedrock на Windows не может подключиться к `localhost` или `127.0.0.1`, выполните:

```powershell
CheckNetIsolation LoopbackExempt -a -n="Microsoft.MinecraftUWP_8wekyb3d8bbwe"
```